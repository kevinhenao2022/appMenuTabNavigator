import { View, Text,} from "react-native";
import { styles } from "../assets/Styles/Main";


//exportart para usar la app
const Contacts = () => {
    return(
      <View style={styles.container}>
        <Text>Inicio</Text>
      </View>
    );
  }

  export default Contacts;